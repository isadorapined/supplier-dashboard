[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# Single-page app: /admin and /password are client-side views.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
